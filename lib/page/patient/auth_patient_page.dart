import 'package:flutter/material.dart';

class AuthPatientPage extends StatelessWidget {
  const AuthPatientPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(child: Text('Auth Patient'),)
    );
  }
}
