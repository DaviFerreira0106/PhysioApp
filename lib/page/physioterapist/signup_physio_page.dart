import 'package:flutter/material.dart';

class SignupPhysioPage extends StatelessWidget {
  const SignupPhysioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('SignUp Physio'),
      ),
    );
  }
}
