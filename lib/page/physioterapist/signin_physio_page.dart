import 'package:flutter/material.dart';

class SigninPhysioPage extends StatelessWidget {
  const SigninPhysioPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('SignIn Physio'),
      ),
    );
  }
}
